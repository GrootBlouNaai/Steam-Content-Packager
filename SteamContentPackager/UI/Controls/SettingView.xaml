<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="SteamContentPackager.UI.Controls.SettingView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:SteamContentPackager.UI.Controls" xmlns:steamContentPackager="clr-namespace:SteamContentPackager" DataContext="{Binding RelativeSource={RelativeSource Self}}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <steamContentPackager:Config x:Key="Config" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid>
    <StackPanel Margin="5">
      <DockPanel Margin="0,0,0,5">
        <TextBlock Width="150" Text="Output Folder" FontWeight="Medium" />
        <TextBox Margin="5,0" Text="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=outputDir}" />
      </DockPanel>
      <DockPanel Margin="0,0,0,5">
        <TextBlock Width="150" Text="Max Connections" FontWeight="Medium" />
        <TextBox Margin="5,0" Text="{Binding MaxConnections}" />
      </DockPanel>
      <DockPanel Margin="0,0,0,5">
        <TextBlock Width="150" Text="Default Language" FontWeight="Medium" />
        <TextBox Margin="5,0" Text="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=defaultLanguage}" />
      </DockPanel>
      <CheckBox FlowDirection="RightToLeft" HorizontalAlignment="Left" FontWeight="Medium" IsChecked="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=deleteDownloads}">
        <FrameworkElement.Resources>
          <ResourceDictionary>
            <Style x:Key="{x:Type Path}" TargetType="{x:Type Path}">
              <Setter Property="FlowDirection" Value="LeftToRight" />
            </Style>
          </ResourceDictionary>
        </FrameworkElement.Resources>
        <ContentControl.Content>
          <TextBlock Width="150" TextAlignment="Left" FlowDirection="LeftToRight" Text="Auto-Delete Downloads" />
        </ContentControl.Content>
      </CheckBox>
      <CheckBox Margin="0,5,0,0" FlowDirection="RightToLeft" HorizontalAlignment="Left" FontWeight="Medium" IsChecked="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=autoSaveBbCode}">
        <FrameworkElement.Resources>
          <ResourceDictionary>
            <Style x:Key="{x:Type Path}" TargetType="{x:Type Path}">
              <Setter Property="FlowDirection" Value="LeftToRight" />
            </Style>
          </ResourceDictionary>
        </FrameworkElement.Resources>
        <ContentControl.Content>
          <TextBlock Width="150" TextAlignment="Left" FlowDirection="LeftToRight" Text="Auto-Save BBCode" />
        </ContentControl.Content>
      </CheckBox>
      <CheckBox Margin="0,5,0,0" FlowDirection="RightToLeft" HorizontalAlignment="Left" FontWeight="Medium" IsChecked="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=autoSaveDepotInfo}">
        <FrameworkElement.Resources>
          <ResourceDictionary>
            <Style x:Key="{x:Type Path}" TargetType="{x:Type Path}">
              <Setter Property="FlowDirection" Value="LeftToRight" />
            </Style>
          </ResourceDictionary>
        </FrameworkElement.Resources>
        <ContentControl.Content>
          <TextBlock Width="150" TextAlignment="Left" FlowDirection="LeftToRight" Text="Auto-Save Depot Info" />
        </ContentControl.Content>
      </CheckBox>
      <DockPanel Margin="0,5,0,5">
        <TextBlock Width="150" Text="Uploader Name" FontWeight="Medium" />
        <TextBox Margin="5,0" Text="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=uploaderName}" />
      </DockPanel>
      <DockPanel Margin="0,0,0,5">
        <TextBlock Width="150" Text="ACF Last Owner" FontWeight="Medium" />
        <TextBox Margin="5,0" Text="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=acfLastOwner}" />
      </DockPanel>
      <DockPanel Margin="0,0,0,5">
        <TextBlock Width="150" Text="BBCode Hoster Name" FontWeight="Medium" />
        <TextBox Margin="5,0" Text="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=hosterName}" />
      </DockPanel>
      <DockPanel Margin="0,0,0,5">
        <TextBlock Width="150" Text="Output Filename Format" FontWeight="Medium">
          <FrameworkElement.ToolTip>
            <ToolTip>
              <TextBlock>Available values: {AppName}, {AppID}, {BuildID}, {Arch}, {OS}, {Language}, {Date}</TextBlock>
            </ToolTip>
          </FrameworkElement.ToolTip>
        </TextBlock>
        <TextBox Margin="5,0" Text="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=outputFilenameFormat}" />
      </DockPanel>
      <DockPanel Margin="0,0,0,5">
        <TextBlock Width="150" Text="Date Format" FontWeight="Medium">
          <FrameworkElement.ToolTip>
            <ToolTip>
              <TextBlock>Use C# String.Format() pluginArguments</TextBlock>
            </ToolTip>
          </FrameworkElement.ToolTip>
        </TextBlock>
        <TextBox Margin="5,0" Text="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=dateFormat}" />
      </DockPanel>
      <CheckBox Margin="0,5,0,0" FlowDirection="RightToLeft" HorizontalAlignment="Left" FontWeight="Medium" IsChecked="{Binding Source={x:Static steamContentPackager:Config.Instance}, Path=runPluginsAsync}">
        <FrameworkElement.Resources>
          <ResourceDictionary>
            <Style x:Key="{x:Type Path}" TargetType="{x:Type Path}">
              <Setter Property="FlowDirection" Value="LeftToRight" />
            </Style>
          </ResourceDictionary>
        </FrameworkElement.Resources>
        <ContentControl.Content>
          <TextBlock Width="160" TextAlignment="Left" FlowDirection="LeftToRight" Text="Run Plugins Asynchronously" />
        </ContentControl.Content>
      </CheckBox>
      <Button HorizontalAlignment="Center" Content="SAVE" Margin="5" Width="75" Click="Save_Clicked" />
    </StackPanel>
  </Grid>
</UserControl>